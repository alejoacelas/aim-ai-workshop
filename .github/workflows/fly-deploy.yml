name: Retired Fly deployment

# The site moved to Vercel and the Fly app was destroyed. Keep this manual-only
# placeholder so the retired deployment cannot run on pushes.
on: workflow_dispatch

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The site is deployed by Vercel."
